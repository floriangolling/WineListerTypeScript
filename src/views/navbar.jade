mixin navLink(link, name)
    a(href=link) #{name}

div(class="topnav")
    +navLink('/','Accueil')
    -if(messages && messages.Co)
                .message.Co
                    +navLink('/vin/add',"Ajout d'un vin")
                    +navLink('/vin/vins',"Voir les vins")
                    +navLink('/stage/addstage', 'Ajouter une semaine')
                    +navLink('/stage/stage',"Stage")
                    +navLink('/logout', 'Se deconnecter')
    -else
        +navLink('/register',"S'enregistrer")
        +navLink('/login','Se connecter')